# Copiez en .env et remplissez

# MySQL
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=souvenirs_a_deux

# JWT (générer une chaîne aléatoire longue)
JWT_SECRET=changez-moi-une-chaine-secrete-longue

# Port du serveur API
PORT=3001

# --- Email (choisir UNE des deux méthodes) ---

# MÉTHODE 1 - Resend (recommandé, très fiable)
# 1. Créer un compte sur https://resend.com (gratuit)
# 2. Dans Dashboard : API Keys → Create → copier la clé
# 3. Domains : ajouter ton domaine OU pour test utiliser onboarding@resend.dev comme EMAIL_FROM
RESEND_API_KEY=re_xxxxxxxxxxxx
EMAIL_FROM=onboarding@resend.dev
# (avec ton domaine vérifié : notifications@mondomaine.com)

# MÉTHODE 2 - SMTP (Gmail, Outlook...)
# Gmail : SMTP_PASSWORD = mot de passe d'application (pas le mot de passe du compte)
# https://support.google.com/accounts/answer/185833
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=votre-email@gmail.com
# SMTP_PASSWORD=xxxx xxxx xxxx xxxx
# EMAIL_FROM=votre-email@gmail.com

# URL de l'application (lien dans l'email)
FRONTEND_URL=http://localhost:5173

# Nom affiché dans l'email (optionnel)
APP_NAME=Souvenirs à Deux
